// Interface definitions for future ARMLS data
// DEFAULT constants and fabricated data removed — will be populated when ARMLS is connected

export type {
  MarketMetric,
  School,
  Restaurant,
  QualityMetric,
  Employer,
  EconomicStat,
  GalleryImage,
  LifestyleFeature,
  KeyDistance,
  Listing,
} from './types';
