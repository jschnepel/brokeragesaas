# RLSIR / Yong Platform — Hands-Off Handoff Snapshot

**Date:** 2026-04-27
**Sign-off intent:** This system runs on its own from here. Audit-grade compliance, self-healing sync, scheduled DQ, alerting wired, build clean.

---

## What's running on its own

### EventBridge schedules (all ENABLED)

| Rule | Cadence | Purpose |
|---|---|---|
| `rlsir-photo-backfill` | rate(20 min) | Fetch photos for new active listings |
| `rlsir-sync-active-schedule` | rate(1 hour) | Pull Active + Pending + Active Under Contract from ARMLS Spark |
| `rlsir-mv-refresh-schedule` | **rate(2 hours)** *(new)* | Refresh analytics_base + 9 downstream MVs |
| `rlsir-purge-raw-data` | rate(2 hours) | Truncate raw_data JSONB on records older than 6 months |
| `rlsir-armls-sync-schedule` | rate(4 hours) | Chronological backlog sync (initial pull + delta) |
| `rlsir-dq-schedule` | **rate(24 hours)** *(new)* | Run 9 DQ assertion rules → write to listing_records_exceptions |

### CloudWatch alarms (wired to SNS)

| Alarm | State | Threshold |
|---|---|---|
| `rlsir-armls-sync-errors` | OK | Lambda Errors ≥ 1 in 5 min |
| `rlsir-rds-storage-critical` | OK | RDS FreeStorageSpace ≤ 4 GB |
| `rlsir-rds-storage-warning` | OK | RDS FreeStorageSpace ≤ 8 GB |
| `rlsir-billing-5` | INSUFFICIENT_DATA | EstimatedCharges > $5 |
| `rlsir-billing-20` | INSUFFICIENT_DATA | EstimatedCharges > $20 |

All actions point to **`arn:aws:sns:us-east-1:828301486081:rlsir-platform-alerts`** (created today; replaces deleted `rlsir-token-alerts`).

### Lambda

- `rlsir-armls-sync` — Node.js 20, 512 MB, 15 min timeout, **`reservedConcurrentExecutions=1`** (eliminates deadlocks).
- Bundle: `infra/lambda/dist/armls-sync.zip` — current sha `eiF0EAD3N7YvhIYoqOPikfIDnnFb4XuyZN8QN7nptxw=` (or whatever the most recent deploy is).
- Tasks supported: `(default)` chronological sync, `sync-active`, `backfill-photos`, `backfill-dom`, `purge-raw-data`, `refresh-views`, **`run-dq`** (new).

---

## Live data state (as of handoff)

| Metric | Value |
|---|---|
| DB total size | 33 GB |
| Active listings | **53,973** |
| Pending | 7,570 |
| Active Under Contract | 5,011 |
| Closed (historical) | 1,830,905 |
| Latest active sync | within last hour (per `MAX(last_synced_at)`) |
| analytics_base rows | ~357,800 (Maricopa, $200K+, 2021+) |
| `mv_dashboard` residential metro | **median $485K, DOM 94d, YoY -0.5%, refreshed 02:51 UTC** |
| `subdivision_canonical_map` | 49,650 mappings (DESERT MOUNTAIN 76→1, SUN CITY 495→1, etc.) |
| `listing_records_exceptions` | 20,552 findings (1,141 error / 36 warn / 19,375 info) |
| `listing_records_excluded` | 0 manual overrides |
| `sync_errors` (Lambda quarantine) | 35 (all PG 22003 numeric overflow, all from before sanitizer) |

---

## ARMLS-compliance posture

| Directive | Status |
|---|---|
| Mirror tables read-only (listing_records, listing_members, listing_offices, listing_open_houses, listing_geography_links, listing_photos, listing_change_log) | ✅ enforced — see `feedback_armls_no_mirror_mutation.md` and `.claude/skills/rlsir-recursive-improvement/SKILL.md` |
| Attribution preserved on IDX surfaces | ✅ — `getListingAgent()` joins on listing_members; `list_agent_full_name`/`list_office_name` in `listing_records` are never overwritten |
| No deletions of MLS data | ✅ — Silver layer uses `listing_records_excluded` LEFT ANTI JOIN, never DELETE |
| Sync recency ≤ 12 h on actives | ✅ — `sync-active` rate(1 hour) keeps Active/Pending/AUC fresh |
| No thin-data fallback in KPIs | ✅ — `findReliableClosed()` skips months with `closed_count < 20`; `getConfidence()` tiers `--` / low / high |

---

## Migration audit

| ID | Name | Applied |
|---|---|---|
| 001 | initial | 2026-02-17 |
| 006 | community_schema | 2026-04-27 |
| 006g | geo_boundaries | 2026-04-27 |
| 028 | drop_listings_mvs | 2026-04-27 |
| 029 | sync_errors | 2026-04-26 |
| 030 | silver_side_tables | 2026-04-26 |
| 031 | dq_idempotency | 2026-04-27 |

Older migrations (002-027) are unregistered legacy. Phase 4 baseline reset is the proper cleanup.

---

## What requires Joey

1. **Confirm the SNS email subscription** — AWS sent a confirmation link to `jschnepel@gmail.com`. Click it. Without confirmation, alarm notifications won't deliver.
2. **Enable AWS Billing Alerts** at the account level — Billing console → Preferences → check "Receive Billing Alerts". Without this, `rlsir-billing-5/-20` stay in INSUFFICIENT_DATA. Account-level toggle, can't be done via API.
3. **Smoke-test `/market` in staging** — the route now builds with stub data layer (broken MVs return empty arrays). Pages render with empty state. When you have time for Phase 4, migrate `/market` to `phoenix-analytics.ts` for real data.

---

## Known issues left for later (Phase 4)

| | |
|---|---|
| `/market` shows empty state | `analytics.ts` + `market-scoped.ts` stubbed via `tryRdsQuery`. Real fix: refactor to use new MV schema (scope_type/scope_key/property_segment). |
| `mtd_closed = 0` and `mos = 9.1` inflated | `mv_dashboard` SQL window logic — fix at next analytics_base rebuild. Self-correcting as sync backfill completes. |
| `yoy_inventory_change = 0` | Hardcoded per the 2026-04-12 plan; "better than wrong." |
| 33 GB DB (toast on raw_data JSONB) | Phase 4 `VACUUM FULL` during maintenance window would reclaim ~10–20 GB. Locks table. |
| Migration baseline reset | ~25 migrations unregistered. Generate `0000_baseline.sql` via `pg_dump --schema-only`, archive old files, repopulate `migrations` table. |

---

## Skill invocation

When picking this back up: invoke the project skill via Claude Code's `Skill` tool — `rlsir-recursive-improvement`. Loaded via `.claude/CLAUDE.md` so any session in this repo sees it. Encodes:
- The 5 ARMLS prime directives
- The 5-step inspect→prioritize→remediate→verify→loop cycle
- ARMLS-safe remediation patterns (derived layer only)
- Performance targets, anti-patterns, rationalization table

---

## Inspector commands

```bash
# Health snapshot
node scripts/db-inspect.mjs

# Silver-layer state
node scripts/silver-inspect.mjs

# DQ rules dry-run (counts only, no writes)
node scripts/dq-assert.mjs --dry-run

# DQ rules live (now also runs daily via Lambda task=run-dq)
node scripts/dq-assert.mjs

# Schema dump for drift detection
node scripts/schema-baseline.mjs
```

## Key file paths

- ARMLS rule pinned: `~/.claude/projects/.../memory/feedback_armls_no_mirror_mutation.md`
- Project state: `~/.claude/projects/.../memory/project_etl_remediation.md`
- Schema baseline: `docs/db/schema-baseline-2026-04-26.{md,json}`
- Migration drift: `docs/db/migration-drift-2026-04-26.md`
- Phase 0 RCA: `docs/db/phase-0-summary-2026-04-26.md`
- This handoff: `docs/db/handoff-2026-04-27.md`
- Skill: `.claude/skills/rlsir-recursive-improvement/SKILL.md`
- Lambda bundle: `infra/lambda/armls-sync-bundle.ts`
- Confidence module: `packages/shared/src/analytics-confidence.ts`
- Silver migration: `packages/database/migrations/rds/030_silver_side_tables.sql`

---

**Sign-off:** as of 2026-04-27, this system is hands-off-ready. The 4-hour and shorter EventBridge cadences keep data current; the 24-hour DQ cycle keeps quality assertions current; alarms fire to SNS → email; the recursive-improvement skill encodes the ARMLS guardrails. Next session can pick up Phase 4 (DBA hygiene, baseline reset, /market refactor) or Phase 5 (yong2 marketing rebuild) without re-deriving today's work.
